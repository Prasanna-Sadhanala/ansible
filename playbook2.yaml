# -name: sp
# host: localhost
# task: collection of the details
# -name: Prasanna


- name: sp
  hosts: frontend
  tasks: #collection of the details
    - name: Prasanna
      debug:
        msg: it is successful with ansible command

- name: Terminate AWS EC2 Instance
  hosts: frontend
  connection: remote
  gather_facts: false

  tasks:
    - name: Delete specific EC2 instance by ID
      amazon.aws.ec2_instance:
        instance_ids:
          - "i-0c6d57a6efd28a1e5"
          - "i-09cc89254712df02a"
        state: absent
        region: us-east-1
  